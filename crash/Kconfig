menu "crash"

config CRASH_UNWIND_DEPTH
    int "size of array where to unwind call-stack"
    default 100

config CRASH_MESAGE_LENGTH
    int "size of array to store a message"
    default 200

endmenu
